<script language="javascript">
function kiem_tra(){
   if($('#ho_ten').val()=='')
   {
      alert('Vui lòng nhập họ tên')
    $('#ho_ten').focus()
    return false
   }
   
   if($('#email').val()=='')
   {
      alert('Vui lòng nhập email')
    $('#email').focus()
    return false
   }
   
   if($('#tieu_de').val()=='')
   {
      alert('Vui lòng nhập tiêu đề')
    $('#tieu_de').focus()
    return false
   }
   
   if($('#noi_dung').val()=='')
   {
      alert('Vui lòng nhập nội dung')
    $('#noi_dung').focus()
    return false
   } 
}

function kt_email(e){
              
      //alert('aa');
      var  dinh_dang = $('#email').val()
      if(dinh_dang.indexOf("@")==-1)
      { 
          alert('Email không đúng định dạng');
          return false;
      }
 }
              
</script>
<%= form_for @product, :url => products_path, :html => {:multipart => true, :class => "form-horizontal product" } do |f| %>

  <% if @product.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @product.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-5">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :image, :class => 'control-label col-lg-2' %>
    <div class="col-lg-5">
      <%= f.file_field :image %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :description, :class => 'control-label col-lg-2' %>
    <div class="col-lg-5">
      <%= f.text_area :description,:rows => 3, :class => 'form-control'%>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :price, :class => 'control-label col-lg-2' %>
    <div class="col-lg-3">
      <%= f.text_field :price, :class => 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary', :onclick= "kiem_tra()" %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                products_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
